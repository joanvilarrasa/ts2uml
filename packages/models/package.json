{
  "name": "@ts2uml/models",
  "version": "0.0.1",
  "module": "index.ts",
  "type": "module",
  "scripts": {
    "clean": "rm -rf .turbo node_modules dist",
    "lint": "biome check --fix .",
    "format": "biome format --write .",
    "build": "biome check --fix . && biome format --write . && bun run src/scripts/update-readme.ts && tsup --clean",
    "testing": "bun index.ts"
  }
}
