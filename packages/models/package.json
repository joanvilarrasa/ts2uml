{
  "name": "@ts2uml/models",
  "version": "0.0.1",
  "module": "index.ts",
  "type": "module",
  "scripts": {
    "clean": "rm -rf .turbo node_modules dist",
    "lint": "biome check --fix .",
    "format": "biome format --write .",
    "build": "bun run scripts/updateReadme.ts && tsup --clean",
    "test": "bun test"
  }
}