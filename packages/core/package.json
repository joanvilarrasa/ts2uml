{
  "name": "@ts2uml/core",
  "version": "0.0.1",
  "module": "index.ts",
  "type": "module",
  "scripts": {
    "clean": "rm -rf .turbo node_modules dist",
    "lint": "biome check --fix .",
    "format": "biome format --write .",
    "build": "tsup --clean",
    "test": "bun test",
    "generate-demo-graph": "bun run src/scripts/generate-demo-graph-json.ts  "
  }
}
